# EuroCalc.eu — Free Financial Calculators for Europe

Live mortgage, VAT, deposit and currency calculators for European users.

## Deploy

Static site — no build step needed.

- **Cloudflare Pages:** set output directory to `public`
- **Netlify:** set publish directory to `public`

## Structure

```
public/
  index.html   ← entire site (single file)
```
